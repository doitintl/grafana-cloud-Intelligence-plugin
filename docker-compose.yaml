services:
  grafana:
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
    ports: !override
      - ${GRAFANA_PORT:-3002}:3000/tcp
      - 2345:2345
    environment:
      DOIT_API_URL: ${DOIT_API_URL:-https://api.doit.com}
      DOIT_API_KEY: ${DOIT_API_KEY:-}
